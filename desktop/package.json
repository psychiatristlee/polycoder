{
  "name": "polyrun",
  "version": "0.1.0",
  "description": "polyrun — a macOS desktop app that edits files & codes via the poly agent",
  "main": "main.js",
  "scripts": {
    "start": "electron ."
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "electron": "^32.2.0"
  }
}
